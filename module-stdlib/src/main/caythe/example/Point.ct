use org.caythe.core.basetypes.Object
use org.caythe.core.basetypes.Integer

class export
delegate Object

export property(get) Integer x
export property(get) Integer y

export new(Integer x, Integer y) {
    this.x = x
    this.y = y
    return this
}
