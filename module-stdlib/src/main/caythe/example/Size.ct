export class

use org.caythe.core.basetypes.Object
use org.caythe.core.basetypes.Integer
use org.caythe.core.basetypes.String

delegate Object

export Integer width { get }
export Integer height { get }

export constructor(Integer width, Integer height) {
    this.width = width
    this.height = height
}

export Integer area() {
    return width * height
}

export String toString() {
    return "[" + width + "x" + height + "]"
}
