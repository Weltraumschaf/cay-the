export class

use org.caythe.core.basetypes.Object
use org.caythe.core.InputStream
use org.caythe.core.OutputStream

delegate Object

export OutputStream stdout { get }
export OutputStream stderr { get }
export InputStream stdin { get }
