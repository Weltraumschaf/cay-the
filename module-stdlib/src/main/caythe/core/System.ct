use org.caythe.core.basetypes.Object
use org.caythe.core.InputStream
use org.caythe.core.OutputStream

object export
delegate Object

export property(get) OutputStream stdout
export property(get) OutputStream stderr
export property(get) InputStream stdin
