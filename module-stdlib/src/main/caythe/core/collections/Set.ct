export
public
class

use org.caythe.core.basetypes.Integer
use org.caythe.core.collections.Collection

<E>

delegate Collection<Integer, E>

export method add(E element) {
    return nil
}

export method E get(Integer index) {
    return nil
}
