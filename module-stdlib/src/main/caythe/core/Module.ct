use org.caythe.core.Version
use org.caythe.core.basetypes.Object
use org.caythe.core.basetypes.String

class export
delegate Object

export property(get) String group
export property(get) String artifact
export property(get) Version version
export property(get) String namespace
