use org.caythe.core.basetypes.Number

export class
delegate Number

@Operator(+)
export Integer add(Integer operand) {
    return 0
}

@Operator(-)
export Integer sub(Integer operand) {
    return 0
}
